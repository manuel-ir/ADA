public class metodos {

    File directorio = new File (directorio, "hijo"); //estructura que declara un archivo File llamado directorio. En el parentesis el parametro que hace 

    .mkdir(); // crea un directorio
    .createNewFile();  // crea un archivo
}
